{
  "name": "@tactico/database",
  "version": "1.0.0",
  "description": "Turso database schema and client for TACTICO",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "init": "ts-node scripts/init.ts",
    "migrate": "node -r ts-node/register src/migrate.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}